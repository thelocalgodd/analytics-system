const siteData = {
  // http://server.url
  siteName: "https://thelocalgodd.me",
  // switch out apiURL to hosted server link
  // send all requests the /api/clean_data
  apiURL: "http:localhost:3002/api/clean_data",
};

export default siteData;
